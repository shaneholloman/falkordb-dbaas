# Cloud Evidence Collector — production ctrl-plane.
# Collects GCP evidence weekly and uploads to GCS evidence locker.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cloud-evidence
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/FalkorDB/falkordb-dbaas.git
    targetRevision: main
    path: argocd/kustomize/cloud-evidence/overlays/ctrl-plane-prod
  destination:
    server: https://kubernetes.default.svc
    namespace: security
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
