apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: security
resources:
  - ../../base
  - secrets-env-secret.yaml
configMapGenerator:
  - name: prowler-config
    behavior: create
    literals:
      - cloud-provider=aws
      - cluster-name=app-plane-aws-prod
      - compliance=soc2_aws
      - wazuh-syslog-host=136.114.5.250
generatorOptions:
  disableNameSuffixHash: true
patches:
  - path: aws-credentials-patch.yaml
  - path: remove-nodeselector-patch.yaml
  - path: irsa-patch.yaml
