# SealedSecret: wazuh-cluster-key
# Contains the Wazuh inter-node cluster key.
# Seal with: kubeseal --format yaml --cert <cert> < secret.yaml
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: wazuh-cluster-key
  namespace: security
spec:
  encryptedData:
    key: "REPLACE_WITH_SEALED_VALUE"
  template:
    metadata:
      name: wazuh-cluster-key
      namespace: security
    type: Opaque
