apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: security
resources:
  - ../../base
  - secrets-env-secret.yaml
configMapGenerator:
  - name: trufflehog-config
    behavior: create
    literals:
      - cluster-name=app-plane-gcp-dev
      - wazuh-syslog-host=34.171.172.83
generatorOptions:
  disableNameSuffixHash: true
patches:
  - path: wi-patch.yaml
