apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: fluent-bit
  namespace: fluent-bit
spec:
  template:
    metadata:
      annotations:
        fluentbit.falkordb.com/gke-parity-config-revision: "2026-05-24"