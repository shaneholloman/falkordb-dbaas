# Service account for the cloud-evidence collector.
# Workload Identity annotation is applied via overlay patches.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: cloud-evidence
  namespace: security
