# Patch ServiceAccount with GCP Workload Identity annotation.
# Uses the same SA as prowler-uploader which has objectCreator on the bucket.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: cloud-evidence
  namespace: security
  annotations:
    iam.gke.io/gcp-service-account: prowler-uploader@ctrl-plane-dev-f7a2434f.iam.gserviceaccount.com
