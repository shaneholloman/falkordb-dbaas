# Remove nodeSelector from CronJob pods — app-plane clusters
# don't have a dedicated "security" node pool.
apiVersion: batch/v1
kind: CronJob
metadata:
  name: kube-bench-cis-scan
  namespace: security
spec:
  jobTemplate:
    spec:
      template:
        spec:
          nodeSelector: null
